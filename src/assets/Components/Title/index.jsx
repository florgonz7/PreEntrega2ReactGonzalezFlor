import React from 'react'

const Title = ({title}) => {
    return (
        <div>
            <h1 style={{textAlign:"center",margin:"1rem"}} >{ title }</h1>
        </div>
    )
}

export default Title